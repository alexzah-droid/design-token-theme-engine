# Commit Policy — Что можно коммитить?

**Цель:** Защита от случайной утечки конфиденциальной информации в git.

**Как работает:** Claude AI читает этот файл перед каждым коммитом и проверяет изменения.

---

## ❌ НИКОГДА не коммитим (автоматически блокируется)

### 1. Внутренняя кухня разработки

```
notes/                  # Личные заметки, TODO списки
scratch/                # Черновики, эксперименты
experiments/            # Эксперименты и тесты
WIP_*                   # Work in progress файлы
INTERNAL_*              # Внутренние документы
DRAFT_*                 # Черновики
```

**Почему:** Это ваша личная кухня, не для публики/команды.

### 2. Framework логи и диалоги (КРИТИЧНО!)

```
dialog/                 # Claude диалоги (могут содержать credentials!)
.claude/logs/           # Framework execution logs
*.debug.log             # Debug логи
debug/                  # Debug файлы
reports/                # Bug reports и migration logs (уже в GitHub Issues)
```

**Почему:** Диалоги могут содержать упоминания паролей, SSH ключей, API токенов в обсуждениях. Bug reports уже отправлены в GitHub Issues, локальные копии не нужны в git.

### 2.1 Framework internal development files

```
BUGS_FIXED_*.md                 # Bug fix documentation (internal)
BUG_FIX_SUMMARY.md              # Bug fix summaries (internal)
COMMIT_POLICY_FIX_SUMMARY.md    # Commit policy fixes (internal)
commit-policy-missing-*.md      # Policy violation reports (internal)
framework-version-*.md          # Version issue reports (internal)
```

**Почему:** Эти файлы - внутренняя документация разработки фреймворка. Bug reports отправляются в GitHub Issues, локальные markdown-отчеты не нужны в публичном репозитории.

### 3. Технические файлы

```
*.local.*               # Локальные конфиги (config.local.js и т.д.)
.env.local              # Локальные environment переменные
.vscode/                # IDE настройки (личные)
.idea/                  # IDE настройки (личные)
*.swp, *.swo            # Vim временные файлы
.DS_Store               # macOS мусор
```

**Почему:** Локальная конфигурация и IDE настройки индивидуальны.

### 4. Генерируемые файлы (если не требуются)

```
dist/                   # Build output (если не нужен в repo)
build/                  # Build output
node_modules/           # Dependencies (уже в .gitignore)
*.log                   # Логи
coverage/               # Test coverage reports
```

**Почему:** Генерируются автоматически, раздувают repo.

### 5. Чувствительные данные

```
secrets/                # Секреты
credentials/            # Credentials
*.key                   # Приватные ключи
*.pem                   # SSL certificates
.production-credentials # Production credentials
backup/                 # Backup файлы (могут содержать данные)
```

**Почему:** КРИТИЧНО - никогда не коммитить credentials!

---

## ✅ ВСЕГДА коммитим (автоматически одобряется)

### 1. Исходный код

```
src/                    # Source code
lib/                    # Libraries
tests/                  # Tests
```

### 2. Публичная документация

```
README.md               # Project README
README_*.md             # Localized READMEs
CHANGELOG.md            # Version history
LICENSE                 # License file
```

### 3. Конфигурация проекта

```
package.json            # npm config
tsconfig.json           # TypeScript config
.eslintrc.*             # Linter config
.prettierrc.*           # Formatter config
```

### 4. Framework метафайлы (публичные)

```
.claude/ARCHITECTURE.md # Если это публичный проект
.gitignore              # Git ignore rules
```

---

## ⚠️ СПРОСИТЬ ПОЛЬЗОВАТЕЛЯ (требует подтверждения)

### 1. Новые папки/файлы не из списков выше

**Критерии:**
- Новая папка, которой нет в "НИКОГДА" и "ВСЕГДА"
- Имя файла содержит: `api-key`, `password`, `secret`, `prod`, `production`
- Файл >1000 строк (может быть generated)

**Что делает Claude:**
Показывает список файлов и спрашивает: "These files are not in COMMIT_POLICY. Add to commit? (y/N)"

---

## 🔧 Настройка под ваш проект

### Добавьте специфичные правила для вашего проекта:

**Пример для бизнес-проекта:**
```markdown
## ❌ НИКОГДА (ваш проект):

business-plans/         # Бизнес планы
competitors/            # Анализ конкурентов
clients/                # Клиентская информация
financial/              # Финансовые документы
```

**Пример для open-source проекта:**
```markdown
## ✅ ВСЕГДА (open-source):

docs/                   # Documentation
examples/               # Code examples
.github/                # GitHub config (workflows, templates)
```

---

## 🔄 Синхронизация с .gitignore

**ВАЖНО:** Все из секции "НИКОГДА" должны быть в `.gitignore`!

**Проверка:** Claude автоматически проверит синхронизацию при запуске `/fi`.

**Текущий .gitignore включает:**
```
# Internal development (from COMMIT_POLICY)
notes/
scratch/
experiments/
WIP_*
INTERNAL_*
DRAFT_*

# Framework logs and dialogs (CRITICAL!)
dialog/
.claude/logs/
reports/

# Framework internal development files
BUGS_FIXED_*.md
BUG_FIX_SUMMARY.md
COMMIT_POLICY_FIX_SUMMARY.md
commit-policy-missing-*.md
framework-version-*.md

# Sensitive data
secrets/
credentials/
*.key
*.pem
.production-credentials
backup/
```

---

## 📊 Как это работает в Completion Protocol

**Step 4 (Git Commit):**

1. Claude читает этот файл
2. Получает список измененных файлов (`git status --short`)
3. Для каждого файла проверяет паттерны:
   - Если файл в "НИКОГДА" → **НЕ** добавляет в commit
   - Если файл в "ВСЕГДА" → Добавляет в commit
   - Если файл в "СПРОСИТЬ" → Спрашивает пользователя
4. Stage только разрешенные файлы (НЕ `git add -A`!)
5. Показывает `git status` для review
6. Спрашивает подтверждение
7. Создает commit

**Защита:** Pre-commit hook блокирует commit если запрещенные файлы всё же попали.

---

## 🚨 Что делать если Claude ошибся?

**Если Claude пытается закоммитить что-то из "НИКОГДА":**
1. Скажите "Нет"
2. Обновите этот файл (добавьте паттерн)
3. Повторите `/fi`

**Если Claude НЕ коммитит нужный файл:**
1. Добавьте паттерн в секцию "ВСЕГДА"
2. Или подтвердите вручную когда Claude спросит

---

## 📝 История изменений

**v2.5.0 (2026-01-17):**
- Первая версия COMMIT_POLICY.md
- Заменён `git add -A` на селективный staging
- Добавлен pre-commit hook для защиты

---

**Редактируйте этот файл под свой проект!**
**Это ваша защита от случайных утечек.**
